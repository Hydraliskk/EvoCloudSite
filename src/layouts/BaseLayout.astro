---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import CookieBanner from "../components/CookieBanner.astro";
import "../styles/global.css";

const {
  title = "Evolution Cloud",
  description = "Services TI, cybersécurité et accompagnement pour les entreprises."
} = Astro.props;

const isLightPalette = Astro.url.searchParams.get("palette") === "light";
---

<!doctype html>
<html lang="fr" data-palette={isLightPalette ? "light-test" : undefined}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" />
    <script is:inline>
      (() => {
        try {
          const url = new URL(window.location.href);
          const palette = url.searchParams.get("palette");

          if (palette === "light") {
            localStorage.setItem("ec_palette", "light-test");
            document.documentElement.setAttribute("data-palette", "light-test");
            return;
          }

          const stored = localStorage.getItem("ec_palette");

          if (stored) {
            document.documentElement.setAttribute("data-palette", stored);
          }
        } catch {}
      })();
    </script>
  </head>
  <body class="min-h-screen text-slate-100 antialiased">
    <Header />
    <slot />
    <Footer />
    <CookieBanner />
    <script>
      if (window.location.search.includes("palette=light")) {
        try {
          localStorage.setItem("ec_palette", "light-test");
        } catch {}
      }
    </script>
  </body>
</html>
